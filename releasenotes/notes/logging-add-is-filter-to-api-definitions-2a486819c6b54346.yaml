---
fixes:
  - |
    Added ``is_filter`` to the ``type`` attribute in the loggable resources
    API definition. All ``logs`` attributes already declared ``is_filter``,
    but ``type`` on ``loggable_resources`` did not, which caused list
    operations with query filters to return HTTP 400 when the
    filter-validation extension is enabled.
