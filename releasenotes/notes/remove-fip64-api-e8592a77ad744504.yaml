---
upgrade:
  - |
    The ``fip64`` API definition and documentation have been removed.
    This extension was only used by networking-midonet, which is no
    longer maintained. The API definition module
    ``neutron_lib.api.definitions.fip64``, its unit test, and the
    corresponding api-ref documentation have all been deleted.
