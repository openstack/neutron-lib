---
fixes:
  - |
    Added ``is_filter`` to the ``id`` and ``tenant_id`` attributes in the
    auto allocated topology API definition. Previously, these attributes
    were not declared as filterable, which caused list operations with query
    filters to return HTTP 400 when the filter-validation extension is
    enabled.
