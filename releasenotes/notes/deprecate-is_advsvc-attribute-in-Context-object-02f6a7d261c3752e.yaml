---
deprecations:
  - |
    Parameter ``is_advsvc`` of the ``context.Context`` object is deprecated and
    will be removed in the future once support for the old RBAC API policies
    will be removed from Neutron.
    Please use ``is_service_role`` instead to force setting Context object as
    service context.
